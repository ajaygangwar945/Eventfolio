<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <defs>
    <radialGradient id="glow" cx="50%" cy="50%" r="50%" fx="50%" fy="50%">
      <stop offset="0%" stop-color="#ffd700" stop-opacity="0.8"/>
      <stop offset="100%" stop-color="#ffd700" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <!-- Background Circle (Night Blue) -->
  <circle cx="16" cy="16" r="16" fill="#0f0c29"/>
  <!-- Glow Effect -->
  <circle cx="16" cy="18" r="8" fill="url(#glow)"/>
  <!-- Hostel Building Silhouette -->
  <path d="M16 8L8 14V24H24V14L16 8Z" fill="#ff9a9e"/>
  <!-- Glowing Window -->
  <rect x="14" y="16" width="4" height="4" fill="#ffd700"/>
</svg>
